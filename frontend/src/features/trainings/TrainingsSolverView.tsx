import { PuzzleSolver, type PuzzleCompletionResult } from './PuzzleSolver';
import { PageHeader, Stat } from './TrainingsViewPrimitives';
import { mistakeLimitOptions } from './trainingsTypes';
import type { CyclePuzzle, CycleStats, Puzzle, Training, TrainingPuzzle } from './trainingsTypes';

type SolverViewProps = { attemptError?: string; attemptIsError: boolean; attemptIsPending: boolean; cycleIsFinished: boolean; cycleStats: CycleStats; currentCyclePuzzle: CyclePuzzle | null; cyclePuzzles: CyclePuzzle[]; failedCyclePuzzleIris: Set<string>; hasActiveCycle: boolean; mistakeLimit: number; mistakeLimitError?: string; mistakeLimitIsError: boolean; mistakeLimitIsPending: boolean; onMistakeLimitChange: (value: number) => void; onBackToDetail: () => void; onPuzzleCompleted: (result: PuzzleCompletionResult) => void; onPuzzleFailed: (result: PuzzleCompletionResult) => void; onPuzzleSelect: (trainingPuzzleIri: string) => void; savedCyclePuzzleIris: Set<string>; selectedPuzzle?: Puzzle; selectedTraining: Training | null; selectedTrainingPuzzle: TrainingPuzzle | null; trainingPuzzles: TrainingPuzzle[]; };

export function SolverView({ attemptError, attemptIsError, attemptIsPending, cycleIsFinished, cycleStats, currentCyclePuzzle, cyclePuzzles, failedCyclePuzzleIris, hasActiveCycle, mistakeLimit, mistakeLimitError, mistakeLimitIsError, mistakeLimitIsPending, onMistakeLimitChange, onBackToDetail, onPuzzleCompleted, onPuzzleFailed, onPuzzleSelect, savedCyclePuzzleIris, selectedPuzzle, selectedTraining, selectedTrainingPuzzle, trainingPuzzles }: SolverViewProps) {
  const currentCyclePuzzleIsSolved = currentCyclePuzzle ? currentCyclePuzzle.status === 'solved' || savedCyclePuzzleIris.has(currentCyclePuzzle['@id']) : false;
  const currentCyclePuzzleIsFailed = currentCyclePuzzle ? currentCyclePuzzle.status === 'failed' || failedCyclePuzzleIris.has(currentCyclePuzzle['@id']) : false;
  const currentPuzzleThemes = selectedPuzzle?.themes ?? [];
  const currentPuzzleRating = selectedPuzzle?.rating ?? null;
  return <div className="wp-page solver-page"><PageHeader eyebrow="Solveur" title={selectedTraining ? selectedTraining.name : 'Aucun entrainement selectionne'} description="Resous les puzzles un par un. Le format suit la convention Lichess." action={<button className="wp-secondary" type="button" onClick={onBackToDetail}>Retour au detail</button>} />{!selectedTraining && <p className="wp-empty">Selectionne un entrainement depuis le tableau de bord.</p>}{selectedTraining && trainingPuzzles.length === 0 && <p className="wp-empty">Cet entrainement ne contient pas encore de puzzle.</p>}{selectedTraining && trainingPuzzles.length > 0 && <div className="wp-solver-layout"><aside className="wp-solver-list"><div className="wp-solver-list-header"><p className="eyebrow">Puzzles</p><div className="wp-solver-list-summary"><Stat label="Total" value={String(trainingPuzzles.length)} /><Stat label="Resolus" value={String(cycleStats.solved)} /><Stat label="A revoir" value={String(cycleStats.failed)} /></div></div>{trainingPuzzles.map((trainingPuzzle) => { const cyclePuzzle = cyclePuzzles.find((item) => item.trainingPuzzle === trainingPuzzle['@id']); const isSolved = cyclePuzzle ? cyclePuzzle.status === 'solved' || savedCyclePuzzleIris.has(cyclePuzzle['@id']) : false; const isFailed = cyclePuzzle ? cyclePuzzle.status === 'failed' || failedCyclePuzzleIris.has(cyclePuzzle['@id']) : false; const className = ['wp-solver-list-item', trainingPuzzle['@id'] === selectedTrainingPuzzle?.['@id'] ? 'active' : '', isSolved ? 'solved' : '', isFailed && !isSolved ? 'failed' : ''].filter(Boolean).join(' '); return <button className={className} key={trainingPuzzle['@id']} type="button" onClick={() => onPuzzleSelect(trainingPuzzle['@id'])}><span>Puzzle {trainingPuzzle.position + 1}</span>{isSolved && <small>Resolu</small>}{isFailed && !isSolved && <small>A revoir</small>}</button>; })}</aside><section className="wp-solver-board-panel"><div className={hasActiveCycle ? 'wp-cycle-status active' : 'wp-cycle-status'}><strong>{cycleIsFinished ? 'Cycle termine' : hasActiveCycle ? 'Cycle actif' : 'Mode libre'}</strong><span>{cycleIsFinished ? `${cycleStats.solved} resolu(s), ${cycleStats.failed} a revoir. Retour au detail pour le bilan.` : hasActiveCycle ? `${cycleStats.solved} resolu(s), ${cycleStats.failed} a revoir, ${cycleStats.pending} restant(s)` : 'Demarre un cycle depuis le detail pour sauvegarder les tentatives.'}</span></div><div className="wp-solver-settings"><span>Tolerance erreurs</span><div>{mistakeLimitOptions.map((option) => <button className={option === mistakeLimit ? 'active' : undefined} key={option} type="button" onClick={() => onMistakeLimitChange(option)}>{option}</button>)}</div></div><div className="wp-solver-focus-card"><div><p className="eyebrow">Puzzle actif</p><h3>{selectedTrainingPuzzle ? `Puzzle ${selectedTrainingPuzzle.position + 1}` : 'Puzzle en chargement'}</h3></div><div className="wp-inline-metrics"><span>{currentPuzzleRating ? `Rating ${currentPuzzleRating}` : 'Rating libre'}</span><span>{currentPuzzleThemes.length > 0 ? currentPuzzleThemes.join(', ') : 'Sans theme'}</span><span>{hasActiveCycle ? 'Tentative suivie' : 'Mode libre'}</span></div></div>{mistakeLimitIsPending && <p className="alert info-alert">Sauvegarde de la tolerance...</p>}{mistakeLimitIsError && <p className="alert error-alert">{mistakeLimitError}</p>}{attemptIsPending && <p className="alert info-alert">Sauvegarde de la tentative...</p>}{attemptIsError && <p className="alert error-alert">{attemptError}</p>}{currentCyclePuzzleIsSolved && <p className="alert info-alert">Ce puzzle est deja sauvegarde comme resolu. Tu peux le rejouer, mais il ne creera pas de doublon.</p>}{currentCyclePuzzleIsFailed && !currentCyclePuzzleIsSolved && <p className="alert warning-alert">Ce puzzle est marque a revoir. Recommence-le pour tenter de le resoudre.</p>}{selectedTrainingPuzzle && selectedPuzzle ? <PuzzleSolver key={selectedTrainingPuzzle['@id']} fen={selectedPuzzle.fen} mistakeLimit={mistakeLimit} onCompleted={currentCyclePuzzle && hasActiveCycle && !currentCyclePuzzleIsSolved ? onPuzzleCompleted : undefined} onFailed={currentCyclePuzzle && hasActiveCycle && !currentCyclePuzzleIsSolved && !currentCyclePuzzleIsFailed ? onPuzzleFailed : undefined} solution={selectedPuzzle.solution} /> : <p className="wp-empty">Chargement du puzzle selectionne...</p>}</section></div>}</div>;
}
