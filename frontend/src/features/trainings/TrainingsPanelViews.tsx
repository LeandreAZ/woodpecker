import { parsePuzzleCsv, type PuzzleCsvRow } from './csvImport';
import { PageHeader, Stat } from './TrainingsViewPrimitives';
import type { Training, View } from './trainingsTypes';

export function DashboardView({ errorMessage, isError, isLoading, onCreate, onOpenTraining, selectedTrainingIri, trainings }: { errorMessage?: string; isError: boolean; isLoading: boolean; onCreate: () => void; onOpenTraining: (trainingIri: string, view?: View) => void; selectedTrainingIri: string | null; trainings: Training[]; }) {
  const selectedTraining = trainings.find((training) => training['@id'] === selectedTrainingIri) ?? null;
  const trainingsWithDescription = trainings.filter((training) => training.description?.trim().length).length;

  return (
    <div className="wp-page">
      <PageHeader eyebrow="Tableau de bord" title="Mes entrainements" description="Reprends un cycle, cree un nouvel entrainement ou ouvre directement le solveur." action={<button className="wp-primary" type="button" onClick={onCreate}>+ Creer un entrainement</button>} />
      {trainings.length > 0 && <section className="wp-dashboard-overview"><Stat label="Entrainements" value={String(trainings.length)} /><Stat label="Selection active" value={selectedTraining ? selectedTraining.name : 'Aucune'} /><Stat label="Descriptifs remplis" value={String(trainingsWithDescription)} /></section>}
      {isLoading && <p className="wp-empty">Chargement des entrainements...</p>}
      {isError && <p className="alert error-alert">{errorMessage}</p>}
      {!isLoading && trainings.length === 0 && <div className="wp-empty-card"><h3>Aucun entrainement pour l'instant</h3><p>Cree ton premier set Woodpecker, puis ajoute des puzzles manuellement ou via CSV.</p><button className="wp-primary" type="button" onClick={onCreate}>Creer le premier entrainement</button></div>}
      {trainings.length > 0 && <div className="wp-training-grid">{trainings.map((training) => <article className={training['@id'] === selectedTrainingIri ? 'wp-training-card selected' : 'wp-training-card'} key={training['@id']}><div className="wp-card-copy"><div className="wp-card-status"><span>{training.status}</span><small>{new Date(training.createdAt).toLocaleDateString('fr-FR')}</small></div><h3>{training.name}</h3><p>{training.description || 'Aucune description pour le moment.'}</p></div><div className="wp-card-meta"><span>{training['@id'] === selectedTrainingIri ? 'Ouvert dans la session' : 'Pret a reprendre'}</span><span>{training.description?.trim().length ? 'Description OK' : 'Description vide'}</span></div><div className="wp-progress"><span /></div><div className="wp-card-actions"><button type="button" onClick={() => onOpenTraining(training['@id'], 'detail')}>Ouvrir</button><button type="button" onClick={() => onOpenTraining(training['@id'], 'solver')}>Solveur</button></div></article>)}</div>}
    </div>
  );
}

export function CreateTrainingView({ description, errorMessage, isError, isPending, name, onDescriptionChange, onNameChange, onSubmit }: { description: string; errorMessage?: string; isError: boolean; isPending: boolean; name: string; onDescriptionChange: (value: string) => void; onNameChange: (value: string) => void; onSubmit: () => void; }) {
  return (
    <div className="wp-page narrow">
      <PageHeader eyebrow="Creation" title="Creer un entrainement" description="Donne un nom clair a ton set. Tu pourras ensuite ajouter ou importer tes puzzles." />
      <form className="wp-form-card" onSubmit={(event) => { event.preventDefault(); onSubmit(); }}><label>Titre de l'entrainement<input maxLength={120} onChange={(event) => onNameChange(event.target.value)} placeholder="Ex : Mat en 2 - avance" required value={name} /></label><label>Description<textarea onChange={(event) => onDescriptionChange(event.target.value)} placeholder="Objectif, niveau, source des puzzles..." rows={6} value={description} /></label>{isError && <p className="alert error-alert">{errorMessage}</p>}<button className="wp-primary" disabled={isPending || name.trim().length === 0} type="submit">{isPending ? 'Creation...' : "Creer l'entrainement"}</button></form>
    </div>
  );
}

export function ImportView({ csvErrors, csvFileName, csvRows, errorMessage, isError, isPending, onFileParsed, onResetFile, onSubmit, puzzleListIsLocked, selectedTraining }: { csvErrors: string[]; csvFileName: string; csvRows: PuzzleCsvRow[]; errorMessage?: string; isError: boolean; isPending: boolean; onFileParsed: (fileName: string, rows: PuzzleCsvRow[], errors: string[]) => void; onResetFile: () => void; onSubmit: () => void; puzzleListIsLocked: boolean; selectedTraining: Training | null; }) {
  const previewRows = csvRows.slice(0, 5);
  const hasImportReadyRows = csvRows.length > 0 && csvErrors.length === 0;
  return (
    <div className="wp-page"><PageHeader eyebrow="Import CSV" title="Importer des puzzles Lichess" description={selectedTraining ? `Import dans : ${selectedTraining.name}` : "Selectionne d'abord un entrainement depuis le tableau de bord."} /><div className="wp-detail-overview-grid"><section className="wp-panel wp-detail-highlight"><div className="wp-panel-title"><p className="eyebrow">Controle</p><h3>{hasImportReadyRows ? 'Import pret' : 'Verification en cours'}</h3></div><p className="wp-detail-highlight-copy">{hasImportReadyRows ? 'Le fichier semble coherent. Tu peux importer ce lot dans le training selectionne.' : 'Charge un CSV puis corrige les erreurs detectees avant import definitif.'}</p><div className="wp-inline-metrics"><span>{csvRows.length} puzzle(s) valides</span><span>{csvErrors.length} erreur(s)</span><span>{selectedTraining ? selectedTraining.name : 'Aucun training cible'}</span></div></section><section className="wp-panel wp-detail-highlight subdued"><div className="wp-panel-title"><p className="eyebrow">Format attendu</p><h3>Points verifies</h3></div><div className="wp-inline-metrics"><span>Moves ou solution obligatoire</span><span>FEN valide si fournie</span><span>Coups UCI legaux</span><span>Doublons detectes</span></div></section></div><div className="wp-two-columns"><section className="wp-panel"><div className="wp-panel-title"><p className="eyebrow">Fichier</p><h3>CSV compatible Lichess</h3></div><p className="muted">Colonnes attendues : <code>PuzzleId,FEN,Moves,Rating,RatingDeviation,Popularity,NbPlays,Themes,GameUrl,OpeningTags</code>. Seule <code>Moves</code> est obligatoire.</p><div className="wp-inline-metrics wp-inline-metrics-spaced"><span>Separateur virgule ou point-virgule</span><span>Notation UCI uniquement</span><span>Les lignes dupliquees sont refusees</span></div>{puzzleListIsLocked && <p className="alert info-alert">Import desactive : un cycle existe deja, donc la liste de puzzles est verrouillee.</p>}<label className="wp-dropzone"><span>Choisir un fichier CSV</span><small>Les coups doivent etre en notation UCI.</small><input accept=".csv,text/csv" disabled={puzzleListIsLocked} type="file" onChange={async (event) => { const file = event.target.files?.[0]; if (!file) { onResetFile(); return; } const result = parsePuzzleCsv(await file.text()); onFileParsed(file.name, result.rows, result.errors); }} /></label></section><section className="wp-panel"><div className="wp-panel-title"><p className="eyebrow">Resume</p><h3>Previsualisation</h3></div><div className="wp-stats compact"><Stat label="Puzzles detectes" value={String(csvRows.length)} /><Stat label="Erreurs detectees" value={String(csvErrors.length)} /></div>{csvFileName && <p className="muted">Fichier charge : <strong>{csvFileName}</strong></p>}{csvErrors.length > 0 && <div className="alert error-alert"><p>Import impossible pour le moment :</p><ul>{csvErrors.map((error) => <li key={error}>{error}</li>)}</ul></div>}{csvErrors.length === 0 && csvRows.length > 0 && <p className="alert info-alert">Le fichier est valide et pret a etre importe.</p>}{previewRows.length > 0 && <div className="wp-import-preview-list">{previewRows.map((row, index) => <article className="wp-import-preview-row" key={`${row.fen ?? 'initial'}-${row.solution.join('-')}-${index}`}><strong>Puzzle {index + 1}</strong><span>{row.solution.join(' ')}</span><small>{row.themes.length > 0 ? row.themes.join(', ') : 'Sans theme'}{' - '}{row.rating ? `Rating ${row.rating}` : 'Rating libre'}</small></article>)}</div>}{isError && <p className="alert error-alert">{errorMessage}</p>}<button className="wp-primary full" disabled={!selectedTraining || puzzleListIsLocked || isPending || csvRows.length === 0 || csvErrors.length > 0} type="button" onClick={onSubmit}>{isPending ? 'Import...' : `Importer ${csvRows.length} puzzle(s)`}</button></section></div></div>
  );
}
